interface:
  display_name: "Superplan"
  short_description: "Pressure-gated persistent planning for Linux"
  default_prompt: "Use $superplan to maintain pressure-gated persistent task checkpoints."

policy:
  allow_implicit_invocation: true
